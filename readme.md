i have built a to do list which can store our data in list form
git init
git status
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/sunilaimlrymec-code/do-to-list.git
git push -u origin main

if any udates source control and do commit for update and later sync with main